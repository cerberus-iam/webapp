# ==============================================
# Cerberus IAM Frontend - Environment Variables
# ==============================================
#
# This file contains example environment variables for the Cerberus IAM Frontend.
# Copy this file to .env.local and fill in the values for your environment.
#
# IMPORTANT:
# - Never commit .env.local or any file containing real secrets to version control
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# - Variables without the prefix are only available on the server side
#
# See README.md for detailed documentation

# ----------------------------------------------
# API Configuration (Required)
# ----------------------------------------------

# Public API base URL - Used by the browser to make API calls
# This must be accessible from the client browser
# Development: http://localhost:4000
# Production: https://api.yourdomain.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Server-side API base URL - Used for server-side API calls
# This can be an internal URL if the frontend and API are in the same network
# Falls back to NEXT_PUBLIC_API_BASE_URL if not set
# Development: http://localhost:4000
# Production: http://internal-api.local or https://api.yourdomain.com
API_BASE_URL=http://localhost:4000

# ----------------------------------------------
# Application Configuration (Required)
# ----------------------------------------------

# Public application URL - The URL where this frontend is hosted
# Used for redirects, OAuth callbacks, and constructing absolute URLs
# Development: http://localhost:3000
# Production: https://iam.yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ----------------------------------------------
# Organization Configuration (Optional)
# ----------------------------------------------

# Default organization slug
# Used when no organization is explicitly selected
# Sent as X-Org-Domain header to multi-tenant APIs
NEXT_PUBLIC_DEFAULT_ORG_SLUG=cerberus-iam

# ----------------------------------------------
# Application Environment (Optional)
# ----------------------------------------------

# Node environment
# Automatically set by Next.js, but can be overridden
# Options: development | production | test
# NODE_ENV=production

# ----------------------------------------------
# Feature Flags (Optional)
# ----------------------------------------------

# Enable debug mode (shows additional logging)
# NEXT_PUBLIC_DEBUG=false

# Enable development tools in production
# NEXT_PUBLIC_DEV_TOOLS=false

# ----------------------------------------------
# Analytics & Monitoring (Optional)
# ----------------------------------------------

# Google Analytics tracking ID
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Sentry DSN for error tracking
# NEXT_PUBLIC_SENTRY_DSN=https://...@sentry.io/...

# ----------------------------------------------
# Development Only
# ----------------------------------------------

# Disable telemetry in development
# NEXT_TELEMETRY_DISABLED=1
